[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  NODE_VERSION = "20"

# Let Netlify auto-detect Next.js runtime without explicit plugin
# The plugin was causing "Failed assembling prerendered content" errors

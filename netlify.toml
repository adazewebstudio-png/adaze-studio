[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  NODE_VERSION = "20"
  # Skip Netlify Forms check - we use Supabase for form handling
  NETLIFY_NEXT_VERIFY_FORMS = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
